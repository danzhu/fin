def test(i Int) Int
    let j Int
    j = if i > 0
        i
    else
        0
    + 1
    j

def fib(n Int) Int
    if n == 0
        0
    else
        if n == 1
            1
        else
            fib(n - 2) + fib(n - 1)

print(fib(20))
