struct Vec
    x Int
    y Int

def distance(v Vec) Int
    v.x * v.x + v.y * v.y

def print(v Vec)
    print(v.x)
    print(v.y)

def test()
    let v Vec
    v.x = 3
    v.y = 4
    if v.x > 0
        (-distance(v) + 1).print()
        0

test()
