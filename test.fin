def test()
    let i Int
    i = -1
    let j Int&
    j := i
    if i < 0
        j += 2
    print(i)

def fib(n Int) Int
    if n == 0
        return 0
    if n == 1
        return 1
    return fib(n - 2) + fib(n - 1)

print(fib(30))
