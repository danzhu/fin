def fib(n Int) Int
    let arr [Int]& := alloc Int[n + 1]

    arr[0] = 0
    arr[1] = 1

    let i = 2
    while i < n + 1
        arr[i] = arr[i - 2] + arr[i - 1]
        i += 1

    arr[n]

def f(n Int) Int
    if n == 0
        0
    elif n == 1
        1
    else
        f(n - 2) + f(n - 1)

def test(n Int)
    fib(n).print()
    f(n).print()

test(30)
