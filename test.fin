def test(i Int) Int
    while if i < 0
        0
    else
        i
    < 5
        i += 1

    i

def fib(n Int) Int
    if n == 0
        0
    else
        if n == 1
            1
        else
            fib(n - 2) + fib(n - 1)

print(test(-2))
