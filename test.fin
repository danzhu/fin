def test()
    let b Bool = FALSE
    if b
        print(0)

    if TRUE
        print(1)

def fib(n Int) Int
    if n == 0
        0
    elif n == 1
        1
    else
        fib(n - 2) + fib(n - 1)

# def list()
#     l.map |e|
#         e->Str
#     .filter |e|
#         e.length > 0

def inc(i Int&)
    i = i + 1

test()
